@using Microsoft.AspNetCore.Identity
@using Library_Management_System.Core.Models


    <ul class="navbar-nav">
        @if (User.Identity.IsAuthenticated)
        {
            @*<li class="nav-item">
                <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @ViewBag.UserName </a>
            </li>*@
            <li>
                <h6> Hello @ViewBag.UserName</h6>
            </li>
            <li class="nav-item">
                <form method="post" id="logoutForm" class="form-inline" asp-area="" asp-controller="account" asp-action="logout">
                    <button id="logout" type="submit" class="nav-link btn btn-link text-dark"> Logout</button>
                </form>
            </li>
            
        }
        else
        {
            <li class="nav-item">
                <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
            </li>

        }
    </ul>